[extend]
useDefault = true

[allowlist]
description = "Teaching examples contain intentionally fake, non-secret values only."
paths = [
  '''(^|/)node_modules/''',
  '''(^|/)dist/''',
  '''(^|/)\.nx/''',
]
